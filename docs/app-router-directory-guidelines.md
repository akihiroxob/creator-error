# App Router directory guidelines

- Keep route entrypoints under `app/`.
- Put route-scoped presentational components under `app/_components/`.
- Keep feature modules under `features/` and import them from route files.
- Keep domain and server layers separated under `domain/` and `server/`.
